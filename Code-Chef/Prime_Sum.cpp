#include<bits/stdc++.h>
using namespace std;
int findprime(int n)
{
    int sum = 0;
    for (int i = 2; sum < n; i++)
    {
        
    }
}
int main()
{
    int t = 1;
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> t;
    while(t--)
    {
        int n, m;
        cin >> n >> m;
        findprime(n);
    }

    return 0;
}