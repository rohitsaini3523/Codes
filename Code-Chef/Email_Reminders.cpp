#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t = 1;
    ios::sync_with_stdio(0);
    cin.tie(0);
    //cin >> t;
    while(t--)
    {
        int n, u;
        cin >> n >> u;
        cout << n - u << "\n";
    }

    return 0;
}