#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    //For Faster input and output
    ios::sync_with_stdio(0);
    cin.tie(0);
    /* cin >> n;
    cin >> m;
    n = n % m;
    if(n<0)
        n += m;
    cout << n << endl; */
    cout << abs(9.999999999) << "\n";
    return 0;
}